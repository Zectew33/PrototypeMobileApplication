<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.part7.angleseahospital">
    <!-- User permission -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AngleseaHospital">
        <activity android:name=".NurseLogout"></activity>
        <activity android:name=".ListCheckingHours" />
        <activity android:name=".CheckingHours" />
        <activity android:name=".NurseLogin" />
        <activity android:name=".RemovePerson" />
        <activity android:name=".EditPerson" />
        <activity android:name=".AddPerson" />
        <activity android:name=".ManagerPinIn" />
        <activity android:name=".Clockout" />
        <activity android:name=".Clockin" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>